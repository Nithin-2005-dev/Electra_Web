export const metadata = {
  title: "Sign In | Electra",
  description:
    "Sign in to access your Electra account, resources, and community features.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
