export const metadata = {
  title: "Dashboard | Electra",
  description:
    "Manage your Electra profile, resources, and orders from your dashboard.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
