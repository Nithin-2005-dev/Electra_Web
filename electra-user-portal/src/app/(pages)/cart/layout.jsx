export const metadata = {
  title: "Cart | Electra",
  description:
    "Review your Electra merch selections and proceed to checkout.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
